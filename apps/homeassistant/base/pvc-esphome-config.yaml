apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-esphome-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-esphome-config
