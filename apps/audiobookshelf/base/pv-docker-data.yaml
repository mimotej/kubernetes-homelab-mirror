apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-audiobookshelf-docker-data
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/nfs/storage/audiobookshelf-kubernetes"
    server: "10.0.0.4"
    readOnly: false
