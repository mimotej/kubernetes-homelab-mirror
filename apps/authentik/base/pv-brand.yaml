apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-authentik-brand
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/nfs/storage/authentik-kubernetes/brand"
    server: "10.0.0.4"
    readOnly: false
