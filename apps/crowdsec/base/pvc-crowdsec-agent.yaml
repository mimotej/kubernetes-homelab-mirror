apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-crowdsec-agent
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeName: pv-crowdsec-agent

