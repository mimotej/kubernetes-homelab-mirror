apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-crowdsec-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: pv-crowdsec-config

