apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-crowdsec-db-dump
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-crowdsec-db-dump
