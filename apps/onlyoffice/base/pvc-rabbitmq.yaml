apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-onlyoffice-rabbitmq
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: pv-onlyoffice-rabbitmq
