apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: onlyoffice-tls
spec:
  secretName: onlyoffice-tls
  dnsNames:
    - "office.mimotej.stream"
  issuerRef:
    name: dns-challenge-issuer-prod
    kind: ClusterIssuer
