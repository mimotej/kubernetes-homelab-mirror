apiVersion: batch/v1
kind: CronJob
metadata:
  name: sql-dump
spec:
  schedule: "0 1,13 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - command:
                - /bin/bash
                - -c
                - |
                  cd /db_dump
                  echo "Deleting dumps older than 14 days..."
                  find /db_dump -maxdepth 1 -mtime +13 -type f -delete
                  DATE=$(date +%Y-%m-%d-%H-%M-%S)
                  echo "Dumping database to /db_dump/nextcloud-$DATE.sql..."
                  kubectl exec nextcloud-postgresql-0 -n nextcloud -- bash -c "PGPASSWORD=$POSTGRES_PASSWORD pg_dumpall --no-role-passwords -U $POSTGRES_USER > /db_dump/nextcloud-$DATE.sql"
                  echo "Dump completed successfully."
                  exit 0
              image: bitnamilegacy/kubectl:1.33.4
              imagePullPolicy: IfNotPresent
              name: job
              env:
                - name: POSTGRES_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-secrets
                      key: db-password
                - name: POSTGRES_USER
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-secrets
                      key: db-username
              volumeMounts:
                - name: nextcloud-db-dump
                  mountPath: /db_dump
          serviceAccount: kubectl-sql-dump
          volumes:
            - name: nextcloud-db-dump
              persistentVolumeClaim:
                claimName: pvc-nextcloud-db-dump
