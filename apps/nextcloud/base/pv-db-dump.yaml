apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-nextcloud-db-dump
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/nfs/storage/nextcloud/db_dump"
    server: "10.0.0.4"
    readOnly: false
