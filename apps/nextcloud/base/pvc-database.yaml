apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nextcloud-database
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: pv-nextcloud-database

