apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-tls
spec:
  secretName: immich-tls
  dnsNames:
    - "immich.mimotej.stream"
  issuerRef:
    name: dns-challenge-issuer-prod
    kind: ClusterIssuer
