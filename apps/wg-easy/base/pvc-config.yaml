apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-wg-easy-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 150Gi
  storageClassName: local-storage
  volumeName: pv-wg-easy-config
