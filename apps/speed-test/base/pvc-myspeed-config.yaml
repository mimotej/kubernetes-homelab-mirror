apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-myspeed-config
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-myspeed-config
