apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-gotify-config
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/nfs/storage/gotify"
    server: "10.0.0.4"
    readOnly: false
