apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-zerobyte-storage-backup
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-zerobyte-storage-backup
