apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-zerobyte-backup-restic
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/storage/backup-restic"
    server: "10.0.0.4"
    readOnly: false
