---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: zerobyte-secrets
spec:
  refreshInterval: 10m
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
  - secretKey: zerobyte-app-secret
    remoteRef:
      key: "zerobyte-app-secret"

