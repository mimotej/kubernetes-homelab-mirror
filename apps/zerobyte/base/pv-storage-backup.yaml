apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-zerobyte-storage-backup
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/storage/backup"
    server: "10.0.0.4"
    readOnly: true
