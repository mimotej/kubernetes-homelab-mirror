apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-zerobyte-docker-data
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ""
  volumeName: pv-zerobyte-docker-data
