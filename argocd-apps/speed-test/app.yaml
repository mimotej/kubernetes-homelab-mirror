---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: speed-test
spec:
  destination:
    namespace: speed-test
    name: production
  project: default
  source:
    path: apps/speed-test
    repoURL: https://github.com/mimotej/kubernetes-homelab-mirror.git
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - Validate=false
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
    automated:
      prune: true
      allowEmpty: true
